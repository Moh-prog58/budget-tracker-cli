# BudgetMaster CLI

A Phase 3 project: CLI budget tracker with SQLAlchemy ORM.

## Setup
pipenv install
pipenv shell

## Run
python -m budgetmaster --help

## Seed Data
python -m budgetmaster seed

## Tests
pytest